class Release {
    //types: 
    // Major, e.g. 3.x -- consider for future, ignore for now
    // Feature, e.g. 3.2.x
    // Maintenance, e.g. 3.2.1
    type: String  
    version: String[]
    releaseDate: String
    constructor() {
    }
}

export default new Release();